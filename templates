<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recherche dans le Document</title>
    <style>
        body { background-color: black; color: lime; font-family: Arial, sans-serif; text-align: center; }
        input { padding: 10px; margin: 10px; font-size: 16px; }
        button { padding: 10px; font-size: 16px; cursor: pointer; }
        .resultat { border: 1px solid lime; margin: 10px; padding: 10px; }
    </style>
</head>
<body>
    <h1>🔍 Recherche dans le Document</h1>
    <form method="POST">
        <input type="text" name="mot_cle" placeholder="Entrez un mot-clé..." required>
        <button type="submit">Rechercher</button>
    </form>
    <h2>Résultats :</h2>
    {% for paragraphe in resultats %}
        <div class="resultat">{{ paragraphe }}</div>
    {% endfor %}
</body>
</html>
